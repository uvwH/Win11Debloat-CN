# Win11Debloat 汉化说明

本文档记录了本仓库相对于上游 [Raphire/Win11Debloat](https://github.com/Raphire/Win11Debloat) 的全部汉化内容、实现机制与维护方法。

---

## 一、概述

汉化分为三层，共覆盖约 **1300 条**用户可见文案：

| 层 | 范围 | 方式 |
|---|---|---|
| 语言包 | GUI 全部界面文案（322 键）、103 个功能描述、12 个分类、10 个选项分组 | 新增 `zh-CN` 语言包，零侵入 |
| 应用目录 | 141 个应用的名称与描述、2 个预设名 | 新增 `AppDetails.json` 覆盖表 + 少量机制代码 |
| 代码层 | CLI 菜单、启动界面、进度/日志/错误消息等约 200 处硬编码英文 | 直接修改 `.ps1` 文案 |

**使用方式**：中文系统（UI 文化为 `zh-CN`）下自动生效；其他系统可加参数强制启用：

```powershell
.\Win11Debloat.ps1 -Language zh-CN
```

任何缺失的键会自动回退到 en-US，界面不会崩溃。

---

## 二、语言包（新增文件）

位置：`Config/Languages/zh-CN/`，键名与 `en-US` 完全一致。

| 文件 | 内容 | 规模 |
|---|---|---|
| `Chrome.json` | 通用界面文案：标题栏、菜单、四个标签页、对话框、还原向导、导入/导出、备份校验消息等 | 322 键 |
| `Features.json` | `Features` 段（103 个功能的 Label/ToolTip/ApplyText/UndoLabel/ApplyUndoText）+ `UiGroups` 段（10 个分组及下拉项） | 约 500 条 |
| `Categories.json` | 12 个分类名称 | 12 条 |
| `AppDetails.json` | **可选覆盖文件**：`Apps` 段按 AppId 映射 `{FriendlyName, Description}`，`Presets` 段按预设英文名映射中文名 | 141 应用 + 2 预设 |

> 注意：语言文件通过 `Import-JsonFile`（显式 `-Encoding UTF8`）读取，**不需要 BOM**。

### 复数键说明

`Chrome.json` 中有 5 组复数键（`_one` / `_other` 后缀），例如 `ApplyCompletionMessageFailures_one`。由于 `Get-PluralCategory` 只实现了英文复数规则（`Count -eq 1` → `one`），中文翻译**必须成对提供**两种键（内容可以相同），否则单数场景会直接显示键名。

涉及键组：`ApplyCompletionMessageFailures`、`ImportExportAppsSelected`、`ImportExportTweaksSelected`、`AppSelectionStatusSelected`、`DeployChangeRemoveApps`。

---

## 三、机制改造（3 个文件）

为支持应用目录的多语言，对 FileIO 层做了最小化扩展：

### 1. `Scripts/FileIO/Import-LanguageFile.ps1`

`Import-LanguageContent` 增加可选加载语言目录下的 `AppDetails.json`（不存在时静默跳过），挂载到返回对象的 `AppDetails` 属性。en-US 目录没有此文件，行为不变。

### 2. `Scripts/FileIO/Import-AppDetailsFromJson.ps1`

加载应用详情时，用 `$script:Lang.AppDetails.Apps` 中匹配的 AppId 覆盖 `FriendlyName` 与 `Description`（对应用的**任一** AppId 匹配即可，兼容 Edge 的双 ID 数组）。无覆盖时使用 `Apps.json` 原值。

### 3. `Scripts/FileIO/Import-AppPresetsFromJson.ps1`

加载预设列表时，用 `$script:Lang.AppDetails.Presets` 覆盖预设显示名称。

---

## 四、代码层汉化（约 44 个 `.ps1`）

以下文件的用户可见文案已直接汉化（注释、变量名、函数名未动）：

- **主入口**：`Win11Debloat.ps1`（启动界面、提权提示、环境校验、winget 警告等 25 处）
- **CLI 全部 8 个文件**（`Scripts/CLI/`）：菜单、默认模式选项、待执行变更清单（`Write-PendingChanges` 的功能标签已改走 `Get-Translation`，配合语言包输出中文）
- **在线启动器**：`Scripts/Get.ps1`
- **核心流程**：`Scripts/Features/` 下 `Invoke-Changes`、`Import-RegistryFile`、`Telemetry-ScheduledTasks`、`Set-StoreSearchSuggestions`、`Replace-StartMenu`、`Invoke-SystemRestorePoint`、`Invoke-RestartExplorer`、`Windows-OptionalFeatures`、`Backup-RegistryState`、`Restore-RegistryBackup`
- **应用移除**：`Scripts/AppRemoval/` 下 `Remove-SelectedApps`、`Invoke-ForceRemoveEdge`
- **GUI 层**：`Scripts/GUI/` 下 `Show-MainWindow`、`Show-ImportExportConfigWindow`、`Show-ApplyModal`、`Show-RestoreBackupDialog`、`Show-RestoreBackupWindow`、`Show-MessageBox`、`MainWindow-AppSelection`、`Get-SystemUsesDarkMode`
- **辅助/文件 IO**：`Scripts/Helpers/` 与 `Scripts/FileIO/` 中的错误与警告文案

未汉化：`Scripts/Run-Tests.ps1`（开发者工具）、`README.md`、全部代码注释。

---

## 五、重要：UTF-8 BOM 编码要求

**所有含中文字符的 `.ps1` 文件必须保存为 UTF-8 with BOM**，否则 Windows PowerShell 5.1 会按系统默认编码（GBK）解析，产生乱码并导致语法解析失败（表现为"表达式或语句中包含意外的标记"，报错位置通常在中文字符串附近）。

当前全部 44 个含中文的 `.ps1` 均已带 BOM。**今后新增或修改这些文件时，务必保持 BOM**。

- 验证方法：文件头字节应为 `EF BB BF`
- 批量补 BOM：

```powershell
$targets = @(Get-Item '.\Win11Debloat.ps1') + @(Get-ChildItem '.\Scripts' -Recurse -Filter *.ps1)
foreach ($f in $targets) {
    $bytes = [IO.File]::ReadAllBytes($f.FullName)
    $hasBom = $bytes.Length -ge 3 -and $bytes[0] -eq 0xEF -and $bytes[1] -eq 0xBB -and $bytes[2] -eq 0xBF
    if (-not $hasBom) {
        $text = [Text.Encoding]::UTF8.GetString($bytes)
        if ($text -match '[^\x00-\x7F]') {
            [IO.File]::WriteAllText($f.FullName, $text, [Text.UTF8Encoding]::new($true))
            Write-Host "BOM added: $($f.Name)"
        }
    }
}
```

- 语法自检（不执行脚本）：

```powershell
foreach ($f in (Get-ChildItem '.\Scripts' -Recurse -Filter *.ps1)) {
    $err = $null
    [void][System.Management.Automation.Language.Parser]::ParseFile($f.FullName, [ref]$null, [ref]$err)
    if ($err.Count) { Write-Warning "$($f.Name): $($err[0].Message)" }
}
```

---

## 六、维护指南

### 上游同步

- **语言包层**（zh-CN 4 个文件）：若上游新增了键，运行对比工具找出缺失项，补译即可：

  ```powershell
  # 加载脚本后调用（缺失/多余键都会列出）
  Test-LanguageKeyCoverage -LanguageCode 'zh-CN'
  ```

- **代码层**：上游对被汉化文件的改动会与中文文案冲突，需按提交逐个合并。这是"写死汉化"方案的固有代价；若冲突频繁，可考虑将代码层文案逐步迁移到 `Chrome.json` 键（走 `Get-Translation`），使 `.ps1` 回归纯英文，便于跟踪上游。

### 新增翻译条目

1. 界面文案 → `Chrome.json` 对应键（XAML 中 `%LANG:Key%` 引用的键必须在 en-US 或 zh-CN 至少一处存在，否则 GUI 启动会抛错）
2. 功能描述 → `Features.json` 的 `Features` 段，键为 FeatureId
3. 应用文案 → `AppDetails.json` 的 `Apps` 段，键为 AppId

### 测试

`Tests/` 中部分测试断言英文输出文案（如 `Invoke-Changes.Tests.ps1`、`Show-CliMenuOptions.Tests.ps1`），汉化后运行测试套件可能存在断言失败，需同步更新这些断言为中文。

---

## 七、已知限制

1. CLI 层文案是直接写入代码的中文，不支持语言切换（改回英文需还原对应 `.ps1`）
2. `Assets/Images/menu.png` 为英文截图，仅作 README 展示，不影响运行
3. 复数规则沿用英文实现，中文场景下单复数文案相同（已通过成对键规避显示问题）
4. 日志文件（`Logs/`）中的时间戳与格式为 PowerShell Transcript 原生输出，未本地化
